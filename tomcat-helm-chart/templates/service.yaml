apiVersion: v1
kind: Service
metadata:
  name: {{ include "tomcat-helm-chart.fullname" . }}
  labels:
    {{- include "tomcat-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 8443
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "tomcat-helm-chart.selectorLabels" . | nindent 4 }}